name: Build and Deploy

on:
  push:
    branches:
      - dev_working
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted  

    steps:
    - name: Checkout the code
      uses: actions/checkout@v2  

    - name: Build and Deploy on Raspberry Pi
      run: |
        cd ~/synth/ZMQProject/ZMQSynth 
        git pull origin dev_working 
        make clean  
        qmake  
        make  

    - name: Restart service running the app
      run: |
        sudo systemctl restart myapp.service 
        sudo systemctl status myapp.service